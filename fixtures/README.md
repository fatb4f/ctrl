# Fixtures

Shared fixture ownership is intentionally conservative in S0. TDD workflow
fixtures remain under `agents/tdd/fixtures/`; semantic mutation fixtures live
next to their canonical CUE packages under `spec/tests/`.

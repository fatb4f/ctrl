# qualification-workflow

Generic, deterministic workflow models, Markdown/CUE parsing,
canonicalization, fixture-manifest derivation, and snapshot compilation.

This package transports and compiles workflow structure. Canonical
qualification semantics remain under the monorepo root `spec/`.

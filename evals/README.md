# Evaluations

Root qualification invokes the committed workflow snapshot evaluation through
`just evaluations`. TDD-specialized evaluation material remains under
`agents/tdd/evals/` until shared ownership is established.
